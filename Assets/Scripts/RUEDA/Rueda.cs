using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rueda : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log($"Hola, soy {this}");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
